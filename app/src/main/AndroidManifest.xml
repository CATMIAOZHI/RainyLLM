<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 网络权限（NanoHTTPd 绑定 127.0.0.1） -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- 前台服务保活 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    
    <!-- 推理时阻止 CPU 休眠 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- 前台服务通知权限（Android 13+） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 悬浮窗权限 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:name=".RainyLLMApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RainyLLM"
        tools:targetApi="31">
        
        <!-- GPU 加速依赖 -->
        <uses-native-library android:name="libvndksupport.so" android:required="false" />
        <uses-native-library android:name="libOpenCL.so" android:required="false" />
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.RainyLLM">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- LLM 推理服务器前台服务 -->
        <service
            android:name=".service.LlmServerService"
            android:foregroundServiceType="specialUse"
            android:exported="false" />
        <!-- 保活通知前台服务 -->
        <service
            android:name=".service.KeepAliveService"
            android:foregroundServiceType="specialUse"
            android:exported="false" />

        <!-- 通知栏按钮广播接收器 -->
        <receiver
            android:name=".service.NotificationActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.rainyllm.app.TOGGLE_FLOATING" />
                <action android:name="com.rainyllm.app.EXIT_APP" />
            </intent-filter>
        </receiver>
    </application>

</manifest>